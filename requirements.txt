# 코어 (플랫폼 무관 — Phase 2 AWS 이전 대상)
pydantic>=2.9
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.9

# 분석 전용 (원본 .hwp 읽기)
olefile>=0.47

# 로컬 전용 어댑터 (Windows + 한컴오피스). AWS 배포 시 제외.
pywin32>=306; sys_platform == "win32"
